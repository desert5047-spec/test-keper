# Supabase（環境ごとに値を設定。本番の値はリポジトリにコミットしない）
EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_or_publishable_key

# 環境ラベル（stg / prod）。アプリ内デバッグ表示に使用
EXPO_PUBLIC_ENV=stg

# Storage バケット名（未設定時は test-images）
EXPO_PUBLIC_STORAGE_BUCKET=test-images

# Web LP URL（アプリから飛ぶ新規登録・パスワードリセット・規約ページなど。環境に合わせて設定）
# stg: EXPO_PUBLIC_LP_URL=https://stg.test-album.jp  （または Vercel Preview URL）
# prod: EXPO_PUBLIC_LP_URL=https://www.test-album.jp
# npm run start:stg / start:prod では script 内で LP_URL を指定済み。.env.stg で上書きしたい場合は dotenv-cli 等を利用可。
EXPO_PUBLIC_LP_URL=https://www.test-album.jp
